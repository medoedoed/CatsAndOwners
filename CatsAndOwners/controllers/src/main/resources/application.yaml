spring:
  application:
    name: security-security
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: "postgres"
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

token:
  signing:
    key: SDFUEI1389FESIF032OIFISHF9832JFDA
    expiration: 86400000